# Data-preparation dependencies (Python >= 3.10). Analysis is in R; see R/install_packages.R
# Pinned: the committed files in data/processed and outputs/ were produced with these versions.
pandas==3.0.5
numpy==2.5.3
